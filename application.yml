spring:
  profiles:
    active:
     - dev

---
spring:
  profiles: dev
  port: 8080
  application:
    name: application

---
spring:
  profiles: test
  application:
    name: microservicecloud-config-atguigu-test
